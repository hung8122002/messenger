/**
 * Base URL cho api của use
 */
const baseURL = "users";

/**
 * api login
 */
const login = baseURL + "/login";

/**
 * api của signup
 */
const signup = baseURL + "/signup";

export { login, signup };
